
<% @rol.each do |rol| %>
<%= link_to "#{rol.name}" , rols_path, remote: true %>
<% end %>

  <div class="container-fluid inner">
  <table class="tableizer-table" id= "simple">
  <tr class="tableizer-firstrow">
    <th>Numero</th>
    <th>Nombre</th>
    <th>otro</th>
  </tr>
      <tr>
        <% @personajes2 = @personajes.sort_by(&:name) %>
        <% @personajes2.each_with_index do |per, index| %>
        <td><%= index+1 %></td>
        <td><%= per.name %></td>
         <td> <ul>
          <%  @rol= Rol.where(personaje_id: per.id) %>
           <% @rol.each do |rol| %>
        <li><%= rol.name %> // <%= rol.pelicula.titulo%></li>
          </ul><% end %>
         </td>
        <td><%= link_to "doble", personaje_path(per), class: 'btn btn-warning', method: :delete, remote: true %>
          <% if per.genero == true %>
            <%= link_to 'mujer', deactivate_personaje_path(per), class: 'btn btn-danger', method: :patch , remote: true %>
            <% elsif per.genero == false %>
            <%= link_to 'hombre', activate_personaje_path(per), class: 'btn btn-primary',
            method: :patch , remote: true %>
            <% end %>
            </td>
         </tr>
         <% end %>
  </table>
